spring:
  application:
    name: eureka-server
server:
  port: 10001
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:10001/eureka